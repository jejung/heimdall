version: "2"
services:
  web:
    domainname: heimdall.dev
    command: python manage.py runserver 0.0.0.0:8000
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ".:/var/www/heimdall.dev/"
